# Make sure that the database exists

Make sure that the database exists

## Usage

``` r
db_setup(package)
```

## Arguments

- package:

  The name of the package under revdep cheking.

## Value

Nothing
